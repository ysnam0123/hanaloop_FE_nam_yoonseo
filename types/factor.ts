export interface Factor {
  id: string;
  name: string;
  activity_type: string;
  scope: string;
  factor_value: number;
  unit: string;
  version: string;
  valid_from: string;
  is_active: boolean;
}
