export function calcEmission(amount: number, factorValue: number): number {
  void amount;
  void factorValue;
  return 0;
}

export function calcCarEquivalent(emission: number): number {
  void emission;
  return 0;
}
